import { AccessPublicMemberDirective } from './access-public-member.directive';

describe('AccessPublicMemberDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessPublicMemberDirective();
    expect(directive).toBeTruthy();
  });
});
